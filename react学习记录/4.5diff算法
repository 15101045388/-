1、state 数据
2、JSX 模板
3、数据 + 模板 生成虚拟DOM（虚拟DOM就是一个JS对象，用它来描述真实DOM）
4、用虚拟DOM 结构生成真实的DOM 来显示
5、state发生变化

6、数据+模板 生成新的虚拟DOM（极大地提升了性能）

7、比较原始虚拟DOM和新的虚拟DOM的区别，找到区别是span中内容（极大地提升了性能）
8、直接操作DOM改变内容

优点：
1、性能提升
2、跨端应用得以实现

setState是异步函数，为了提升底层的性能

虚拟DOM的同层比对

如果产生了差异，，就把发生变化的那一层直接替换。不再去比对下面的DOM




