
区分大小写
linux 命令
ls -a 查看所有目录 包括隐藏目录   隐藏目录的文件名称前面有 .

创建文件 mkdir

复制目录 cp -R 要复制的文件 复制后的文件名称
cp -R aaa bbb

删除文件
rm 文件名称
删除文件目录
rm -r 文件目录名称

pwd命令可以查看当前所在目录

window
dir 查看当前文件夹下的目录 有dir标记的是文件夹，没有标记的是文件
cd 进入目录
